name: My sample learning workflows 
on:
  push:
    branches: [ wrkflows]
    

jobs:
  Build:
    name: Build JOB
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: testing 
        run: |
          echo "hello"
